# 更新日志 · 黄钰茜 个人作品集

> 本文件记录网页 `portfolio/index.html` 的迭代变更，便于后续维护。

## 2026-07-31 · 整站重设计
- **重构 · 首屏瀑布流**：将原有 Hero 改为两页瀑布流。第一页为全屏双语欢迎语「Hi, welcome to. / 你好，欢迎来到黄钰茜的世界」+ 现有头图；向下滚动自动吸附到第二页教育经历。
- **重构 · 导航目录**：参考示例图样式，改为顶部汉堡按钮 + 全屏覆盖式大文字菜单，按钮顺序为 Internship / Portfolio / Honors / About me / Contact me，全部中英双语。
- **重构 · 实习经历**：移除原 3D 金字塔，改为垂直时间线。时间线自下而上按「由远到近」排列 8 段实习经历；点击节点从右侧滑出详情面板，内含真实工作描述与项目成果。
- **重构 · 作品集**：改为上方并排两个「工具箱」按钮——内容创作类 / AI 能力类；点击切换下方内容区域。内容创作类展示文章、报道、论文、提案等；AI 能力类展示腾讯 AI 商业分析案例与个人 AI 工具箱。
- **保留 · 荣誉证书书架**：维持书架式交互，3 本书（竞赛与表达 / 学业表现 / 志愿与社会服务），点击书脊展开证书，点击证书打开 PDF。
- **重构 · 关于我**：新增 MBTI ESFJ 徽章，保留 6 项兴趣爱好并改为照片墙占位布局，提示后续插入真实照片。
- **新增 · 联系我**：新增独立 Contact me 区块，含邮箱、简历下载、回到顶部。
- **优化 · 双语标题**：所有板块主标题均改为「中文 / ENGLISH」双语形式，例如「荣誉证书 Honors」。

## 2026-07-30
- **修复**：荣誉证书区块不显示。根因是证书分组由 JS 动态生成、未被滚动显隐用的 `IntersectionObserver` 接管，已在每组追加后补 `io.observe(group)`。
- **新增 · 作品集联动**：将"作品产出"嵌入三维能力金字塔。点击任意一层（顶层 AI 产业分析 / 中层 沟通与项目管理 / 底层 文字功底与内容创作），金字塔左移、右侧展开该实习经历下的具体作品（数据见脚本中 `TIER_WORKS`）。
- **新增 · 个人提效 AI 作品集**：在金字塔下方单列"个人提效 AI 作品集"区块，带"持续更新中"呼吸灯标识（数据见 `AI_WORKS`）。
- 触发背景：网页定位为"个人简历 & 作品集"，但此前作品未在页面体现。

## 2026-07-30（续）
- **修复 · 折叠留白 bug**：点击金字塔某层展开作品面板后再收回，面板原本的 DOM 内容仍在、且 `align-self:stretch` + `width:0` 会让内容竖向堆叠把容器撑高，导致收回后中间空一大块。现把折叠态的 `.works-panel` 改为 `height:0; align-self:flex-start`（展开态 `height:auto; min-height:480px`），彻底消除折叠后的空白。移动端同步把 `min-height:auto` 改为 `min-height:0`。
- **改名 · 旅行轨迹 → 兴趣爱好**：导航与区块标题由"旅行轨迹 / Travel Map / 从兰州出发"改为"兴趣爱好"（旅行只是其中一项）。同时移除原 ECharts 中国/世界放射地图（含 CDN 脚本与相关 JS/CSS），避免与"旅行"贴图重复且依赖联网 GeoJSON。
- **新增 · 兴趣爱好贴图墙**：在 `hobby-sec` 注入 6 张 scrapbook 风格的图片占位贴图（旅行 / 乒乓球 / 摄影 / 阅读 / 美食 / 音乐），带胶带、轻微旋转、虚线占位框与"＋ 点击此处替换照片"提示。数据见脚本 `HOBBY_STICKERS`；插入真实照片方法见该处注释（把 `.sticker-img` 内容换成 `<img>` 并加 `class="has-img"`）。

## 2026-07-30（配色 / 尖顶 / 链接）
- **整体配色改浅蓝调亮**（#7）：`:root` 令牌由深紫黑转浅蓝/蓝白——`--bg:#eaf2fb`、`--surface` 改半透明白、`--text` 改深海军蓝、`--neon:#2f6fed`（蓝），保留 `.bg-orb` 光晕质感；同步把导航玻璃、软技能节点、金字塔面/激活态、tier 文字与 chip、英雄 badge、AI badge 等硬编码紫色刷成蓝。证书卡与灯箱保持深色"画框"质感形成对比，仅修正灯箱说明文字颜色为白。
- **金字塔补尖顶**（#5a）：三层改为连续三角形——宽度 100%/66.7%/33.3%、各高 144px、clip-path 收尖（顶层为三角 `polygon(50% 0,100% 100%,0 100%)`），层间距 gap 改 0，顶层内容底部对齐适配尖顶。现为完整尖顶金字塔。
- **修复未展开间距**（#5b）：`.pyramid-stage` 的 `min-height` 由 610px 降到 470px（金字塔本体 432px），消除未点击时金字塔与下方内容间的过大留白。
- **消除展开重叠**（#2）：展开态 `.pyramid-stage` 左移由 `-34px` 改 `0`，避免展开时金字塔压到左侧软技能节点（面板从右侧滑入）。
- **挂上 5 条中心宣传稿件链接**（#4）：新增 `#prLinks` 区块（"中心宣传稿件"），由脚本 `PR_ARTICLES` 注入 5 张可点击卡片（角色标签 + 标题 + "查看原文 ↗"，`target=_blank` 外链微信公众号文章）。增删链接直接改 `PR_ARTICLES` 数组。

## 2026-07-31（下午）· 二次调整
- **恢复 · AI 能力 Skill/MCP/Agent**：保留三个项目卡片（腾讯 AI 方向商业分析 / HTML 动态数据看板 / 行业分析标准化流程）的同时，恢复下方的 Skill / MCP / Agent 三个维度模块；删除「腾讯：AI × 商业分析」引言卡片。
- **调整 · 荣誉证书横排**：将原来纵向的书架改为三本书横向并排，点击每本书的书脊在卡片下方展开证书列表。
- **重构 · 关于我兴趣爱好（更紧凑）**：
  - 旅行：改为全宽卡片，用 SVG 绘制从兰州出发到世界各地的动态连线地图，标注兰州、北京、上海、成都、中国香港、中国澳门、中国台湾、东京、首尔、美国；线条有依次生长动画。
  - 摄影：改为规整的 3×2 照片墙占位。
  - 阅读：删除书籍照片和书架占位；累计阅读书本改为从 0 递增到 100+ 的数字动画，最近最爱只保留《智能简史》。
  - 美食：改为 3×2 美食照片墙占位。
  - 乒乓球：保留 CSS 动画球台，压缩在右侧卡片中。
  - 音乐：单独一行小卡片，纯文字。

## 2026-07-31（傍晚）· 第三次调整
- **教育经历课程**：从简历 docx 提取真实课程写入教育卡片——硕士（国际关系，全英授课）：经济学原理、中美比较经济、人工智能通识与应用、能源资源与环境经济学、能源技术与政策；本科（北师大汉语言文学）：牛津 OPGDI 核心课程（语言学概论、经济全球化与当代中国经济、比较视野中的中国政策过程、现代公共关系实务）。
- **新增 · 软技能区块**：放在实习时间线下方，三张卡片——领导力（白鸽青年志愿者协会宣传讲解中心主任 / 北京市科研创新计划队长）、演讲表达（Model APEC 最佳政策提案、杰出代表）、沟通协调（中美中心公关助理，曾带队高中教师成人班级）。
- **荣誉证书改为可视书架**：删除点击展开交互，三本书横向并排直接呈现所有证书，底部加木质书架视觉效果；点击证书仍打开 PDF。
- **删除 · 乒乓球与音乐**：从关于我中移除，关于我现保留旅行 / 摄影 / 阅读 / 美食四项。
- **姓名统一为「黄玉倩」**：简历页眉邮箱拼音 `huangyuqian` 与用户要求一致，故把全站 `黄钰茜 → 黄玉倩`、`Huang Yuxi → Huang Yuqian`。
- **联系我**：补上真实联系方式——邮箱 `huangyuqianamy@163.com`、电话 `13893283875`，并接入简历 PDF 下载。
- **生成简历 PDF**：用 reportlab 把「咨询-黄钰茜的简历.docx」转为 `assets/黄玉倩的简历.pdf`（命名不含"咨询岗"）。
- **作品集内容创作微调**：「电力赋能乡村振兴」背景由山区茶叶厂改为四川郫县豆瓣酱；「中美中心」不再链接公众号/视频号主页，改为待补充的具体作品链接。

## 待办 / 可继续完善
- 替换摄影、美食的真实照片（当前为占位符）。
- 更新「累计阅读书本」最终数字（当前使用占位值 `100+`）。
- 用户稍后提供内容创作类（尤其是中美中心）的具体作品外链后，补充进 `CONTENT_WORKS` 的 `links`。
- 如需中英双语课程对照或补充教育页独立照片（`hero.jpg` 可替换），可继续调整。

---
格式说明：每次改动请追加一个带日期的小节，并尽量标注"新增 / 修复 / 调整"类型。
